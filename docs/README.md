# PlayNest 文档

- `planning/`：长期方向、当前状态、任务与路线。ChatGPT 接力优先使用 `GPT_PLANNING_BRIEF.md` 和 `AI_HANDOFF.md`。
- `product/`：产品定位、体验原则、游客模式与候选目录。
- `architecture/`：当前技术结构、数据模型、游戏接入目标与未来在线架构。
- `development/`：代码约定、测试、决策与排障手册。
- `history/`：只追加的开发记录和用户可见变化。

每类内容只有一个权威文件，不创建带 `NEW`、`V2` 或日期后缀的重复文档。
